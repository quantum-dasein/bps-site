# /public/images

Drop your final custom-generated images here using the EXACT filenames below.
The code currently uses Unsplash URLs as premium placeholders. Once you place
these files, swap the `image:` URLs in `components/Geography.tsx` (and any hero
background) to local paths like `/images/uae-office.webp`.

Expected filenames:
- hero-bg.webp        — ambient dark/gold hero backdrop (optional)
- cis-market.webp     — CIS / СНГ business region card
- uae-office.webp     — UAE / ОАЭ region card
- asia-market.webp    — China & India region card
- texture-gold.webp   — subtle gold texture overlay (optional)
